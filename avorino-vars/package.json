{
  "name": "avorino-vars",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run watch\" \"webflow extension serve\"",
    "watch": "tsc --watch",
    "build": "tsc",
    "bundle": "npm run build && webflow extension bundle"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@webflow/designer-extension-typings": "^2.0.27"
  }
}
